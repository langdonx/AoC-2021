{
    "version": "0.2.0",
    "configurations": [
        {
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "name": "Launch Program",
            "program": "${workspaceFolder}\\main.mjs",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "pwa-node"
        }
    ]
}